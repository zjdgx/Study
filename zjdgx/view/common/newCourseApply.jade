//
   Created by nhn on 15/10/21.
html
   head
      include ../common/header
      link(rel="stylesheet", href="/dist/teacher.min.css")
   body
      include ../common/head
      div.page.newCourse
         div.item.subject
         div.item.studentStatus
            label 学员情况:
            textarea(rows=3)
         div.item.location
            label 所在区域:
         div.item.contactName
            label 联系人:
            input(type="text", maxlength="20")
            span.hint 例如: 王先生
         div.item.contactPhone
            label 联系电话:
            input(type="number", maxlength="14")
         div.item.notes
            input(type="checkbox", checked)
            span 因上述信息不真实而引起的任何后果，一概由填表人负责
      div.footer
      script(type="text/javascript", src="/js/common/require.min.js")
      script.
         require(['jquery', 'zjdgx'], function ($, Common) {
            Common.loadNewCourse();
         });